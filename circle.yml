# dependencies:
  # post:
  #   - go get github.com/google/go-github/github

# env:
#   - GITHUB_API_TOKEN=hello
#   - ARTIFACT_DIR=./

test:
  pre:
    - go build -o citask citask.go

  override:
    - ./citask --context test1 --description "tests" "sleep 30"
    - ./citask --context test2 --description "tests" "sleep 30 && echo done"
    - ./citask --context test3 --description "tests" "sleep 30 && echo done"
    # - ./citask finish
