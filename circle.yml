dependencies:
  pre:
    - go get github.com/google/go-github/github

test:
  pre:
    - go build .

  override:
    - ./ci-test --context test1 --state pending && sleep 30 && ./ci-test --context test1 --state success
    - ./ci-test --context test2 --state pending && sleep 30 && ./ci-test --context test2 --state success
    - ./ci-test --context test3 --state pending && sleep 30 && ./ci-test --context test3 --state success
